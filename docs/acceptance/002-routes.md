# 第二批驗收：路線／結局資料化與文件一致性

日期：2026-09-13。任務：S0-03、S1-02。狀態：已於 2026-09-13 由使用者回覆「繼續吧」確認。

## 交付

- `routes` 明確列出每位角色的事件順序，HUD 由已完成事件計算進度，不再拼接事件名稱或固定三階段。
- 核心規則限制路線事件依序開啟；即使後續事件高優先度且其他條件成立，也不能跳過前面的事件。
- `endings` 用非空 AND 條件判定完成提示，不再依六個完成事件判定結局。
- 紀念卡內容／解鎖條件資料化，取消 a/b 與第三階段假設；完整媒體畫廊仍未完成。
- HUD 改為可捲動角色卡片，避免角色數量增加時直接溢出。
- 規格 HTML／MD 更新至 v0.6，加入任務對照及已讀行為說明；README 移除過期說法。

## 驗證證據

`python tools/dev.py check`：26 項通過，包含原正常滑鼠六事件通關及存讀檔回歸。

新增 fixture：ivy／mira／noa 三位角色各有 1／2／4 個事件，事件使用 arrival、lost_ribbon 等非數字命名。驗證順序限制、第六事件不誤判結局、第七事件完成指定結局、紀念卡解鎖、新遊戲重置及讀檔重建進度。另以狀態 fixture 驗證提高數值 stage 不能代替完成事件。

驗證器涵蓋事件不存在、重複分配、角色不符、空結局條件，以及路線順序與 completed 條件形成的循環。

實際 renderer 執行 `ui-smoke` 與 routes_test，檢查 demo 與三角色進度畫面；`test-results/routes-start.png`、`routes-end.png`、`game.png` 為本機產物。NVIDIA RTX 5070／Compatibility 運作正常；Windows 根憑證讀取警告仍存在，無腳本錯誤。

## 限制與下一步

此案例是測試 fixture，不算第二款可交付遊戲。結局仍是文字提示，不含片尾演出。無 routes 的角色沒有路線進度；獨立事件仍按其條件／優先度執行。完整 Schema、主題／主角配置與正式畫廊分別保留在 S2、S1-03、S4-08。

下一批：S1-03 主題、主角 ID、肖像與字型／配色配置，以及 S1-04 模組邊界。S1 尚未全數完成。
